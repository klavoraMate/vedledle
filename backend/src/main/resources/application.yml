spring:
  datasource:
    url: jdbc:postgresql://vedledle-instance-1.cvaygozdccix.eu-north-1.rds.amazonaws.com:5432/vedledle
    username: postgres
    password: postgresvedledle
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org.hibernate.SQL: debug
